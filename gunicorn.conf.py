# gunicorn.conf.py

# Worker timeout setting (in seconds)
timeout = 120

# Number of workers (this depends on the expected load)
workers = 2

# Optionally, other Gunicorn configurations like logging, worker class, etc.